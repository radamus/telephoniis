
<%- ContactServices.contactForm({
	title : 'Edytuj kontakt',
	submitText: 'Zapisz zmiany', 
	submitAction : '/contact/update/'  + contact.id, 
	cancelText : 'Anuluj', 
	cancelAction: '/contact/show/'+ contact.id, 
	csrf: _csrf, 
	contact: contact,
	flash : flash
	}) %>